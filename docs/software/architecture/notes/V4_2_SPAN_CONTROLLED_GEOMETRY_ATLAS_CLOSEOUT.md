# V4.2 span-controlled geometry atlas closeout

**Disposition:** generated; non-inferential retained evidence
**Implementation / generation revision:** `ce9afb8f37cf86c82931c194144a2c239e6b762e`
**Atlas package:** [`results/v4_review/v4_2_span_controlled_geometry_atlas/`](../../../../results/v4_review/v4_2_span_controlled_geometry_atlas/)
**Config digest:** `19e5075ab362aa8cfe1013b0bec810b4d2443f81fbf707ed33342b4a8433696a`
**V3.6D registry digest:** `456efd9f9472f8cee6271347e4e13bc750473bc186f752a254c526cc853296f0`
**Work packages closed:** V4-200 through V4-208
**Grid:** \(33\times 33\) shared \(Q\) per case (17 cases, 18,513 samples, 55,539 rows, 0 failed)
**Later drafting:** a sibling visual-planning audit (V4.2A) and Sprint V4.3 do not reopen this package. No V4.2 defect review or regeneration is authorized by those later sprints.

## Review conclusion

Sprint V4.2 consumes the frozen V3.6D registry by digest over 17 unique ordered span assignments. Each case evaluates:

- the V3.6D four-bar operating branch;
- its span-matched affine gearbox over the same usable \(U\) and \(Q\) intervals;
- identity-on-shared-\(Q\) as a null control, not a ranked competitor.

Every atlas row is a V4.0 `geometry_snapshot`. Rank attribution distinguishes \(J_g\), \(J_f\), and \(J_{xu}\). Identity \(J_g=I\) on every sample. 175° remains `boundary_stress_only`. `(145°, 145°)` has one physical record and two matrix placements. The HTML states **intrinsic geometry atlas; no mechanism performance inference.** Frozen V3 packages and the V4.0/V4.1 packages were not rewritten.

This sprint described transmission geometry. It did not rank mechanisms, run application tasks, compute wrench polytopes, or retune `PRIMARY_CERTIFICATE`.

## Authorization

`ACTIVE_SPRINT.md` returns to **no code authorization**. Activating a sibling visual-planning audit or Sprint V4.3 requires a separate reviewed change. V4.2 completion does not authorize later sprints.
